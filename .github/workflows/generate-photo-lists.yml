name: Auto-generate photo lists for gallery

on: [push, workflow_dispatch]

jobs:
  generate-lists:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # ← This is REQUIRED for git-auto-commit to push

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Generate lex2026SeniorPics photo list
        run: |
          pwd
          ls -la

          cd "Photography Grid/Lex2026SeniorPics" || { echo "CD failed!"; ls -la ..; exit 1; }

          pwd
          ls -la

          # Find all image files (case-insensitive)
          files=$(find . -maxdepth 1 -type f \( -iname "*.webp" -o -iname "*.jpg" -o -iname "*.jpeg" -o -iname "*.png" \) -printf '%f\n' | sort || true)

          if [ -z "$files" ]; then
            echo "No images found"
            exit 0
          fi

          echo "Found images:"
          echo "$files"

          # Generate in ROOT (../../../ from current deep folder)
          echo "const lex2026Images = [" > ../../../lex2026-images.js
          for file in $files; do
            echo "  '$file'," >> ../../../lex2026-images.js
          done
          echo "];" >> ../../../lex2026-images.js
          echo "export default lex2026Images;" >> ../../../lex2026-images.js

          echo "Generated lex2026-images.js successfully"

      - name: Commit generated file
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: Auto-generated lex2026 photo list update
          file_pattern: lex2026-images.js  # Exact name - no wildcard
          disable_globbing: true
          branch: main
